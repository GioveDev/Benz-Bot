1.Install python
2.Install ffmpeg and make it usable systemwide (Windows: Add to path Environment variable) (Linux: i don't fucking know, you should know how to use something systemwide)

3.Create a file called .env inside the same folder as BenzBot V2.py, copy the following inside and get the token from #coding

  # .env
  DISCORD_TOKEN=Token Comes Here!

4.pip install all necessary dependencies

5.then run this line:
python "BenzBot V2.py"
