1.Install python
2.Install ffmpeg
3.Create a file called .env inside the same folder as BenzBot V2.py, copy the following inside and get the token from #coding
  # .env
  DISCORD_TOKEN=Token Comes Here!

4.Install the chromium webdriver with sudo apt-get install chromium-chromedriver

5.pip install all necessary dependencies

6.then run this line:
python "BenzBot V2.py"
